[model]
name = Roadmap Card
label = {{ this.title }}
hidden = yes

[fields.column]
label = Column
type = select
choices = current, near-term, future, completed
choice_labels = Current, Near-Term, Future, Completed

[fields.category]
label = Category
type = select
choices = Development, Projects, DevOps, General, Outreach

[fields.title]
label = Title
type = string
size = large

[fields.body]
label = Body
type = markdown

[fields.tags]
label = Tags
type = strings
description = List of tags

[fields.sort]
label = Sort key
type = sort_key

[fields.year]
label = Year
type = integer
